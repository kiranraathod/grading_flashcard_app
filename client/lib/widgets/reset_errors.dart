import 'package:flutter/material.dart';

/* 
This file exists solely to force a rebuild of the project to clear any 
cached errors. If you're still seeing BorderStyle.dashed errors after 
updating code, try running:
  flutter clean
  flutter pub get
  flutter run
*/

class ResetErrors extends StatelessWidget {
  const ResetErrors({super.key});

  @override
  Widget build(BuildContext context) {
    // This widget is never used in the actual app
    return const SizedBox.shrink();
  }
}
